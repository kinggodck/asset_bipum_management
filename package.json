{
  "name": "yg-asset-qr-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel-ready static app for asset and supplies QR management.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "python3 -m http.server 4174 --bind 127.0.0.1",
    "preview": "python3 -m http.server 4175 --bind 127.0.0.1 --directory dist"
  },
  "devDependencies": {}
}
